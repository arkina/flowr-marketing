{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'Contact'}, 'Contact') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>{{ 'Contact' | trans({}, 'Contact')}}</h1>
        <ol class="breadcrumb">
            <li><a href={{ path('dashboard') }}><i class="fa fa-dashboard"></i> {{"Dashboard" | trans({}, 'Contact')}}</a></li>
            <li><a href="{{ path('contact') }}"> {{ '%entity% List'|trans({'%entity%': 'Contact'}, 'Contact') }}</a></li>
            <li class="active" >{{ 'New %entity%'|trans({'%entity%': 'Contact'}, 'Contact') }}</li>
        </ol>
    </section>        
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'Contact'}, 'Contact') }}</h3>
            </div>
            <div class="box-body">
                {{ form_start(form, {action: path('contactlist_contact_create', {id: contactList.id})}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'Contact') }}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions">


        </div>

            </div>
        </div>
    </section>
{% endblock %}
